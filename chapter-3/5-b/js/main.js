// 2つの数値を足す関数
let add = function(arg1, arg2) {    // 引数「arg1」「arg2」をとる
    let sum = arg1 + arg2;      // 計算処理をおこなう
    return sum;                 // 戻り値を返す
};

let res = add(100, 200);        // 関数を利用
console.log(res);               // 「300」とコンソールに表示
